type: object
properties:
  queryInfo:
    $ref: './QueryInfo.yaml'
  cursorInfo:
    $ref: './CursorInfo.yaml'
  zones:
    type: array
    items:
      $ref: './Zone.yaml'
    description: List of the returned zones. Each item in the list matches the zone DTO described above.
required:
  - zones
